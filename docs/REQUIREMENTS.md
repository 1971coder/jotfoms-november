# Behavioral Notes (ShiftNotes) Processor — Requirements

## 1. Goal
Process **Jotform ShiftNotes emails** stored as `.eml`/`.html` files in a local folder, extract structured data, store it, and generate simple analytics and AI summaries **fully offline**.
